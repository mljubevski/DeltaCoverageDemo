package com.example.deltacoveragedemo.util

import org.junit.Test

class SomeUtilTest {
    @Test
    fun `verify some util returns true`() {
        assert(
            value = SomeUtil.printSomething()
        )
    }
}